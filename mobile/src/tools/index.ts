// import * as RNLocalize from "react-native-localize";

// export function getDeviceLocale() {
//     const DEFAULT_LANGUAGE = 'pt-BR';
//     const locales = RNLocalize.getLocales();
//     const listLocale = locales.map(i => i.languageTag) || [];
//     const bestLocale = RNLocalize.findBestAvailableLanguage(listLocale);
//     return (bestLocale && bestLocale.languageTag) || DEFAULT_LANGUAGE;
// }
